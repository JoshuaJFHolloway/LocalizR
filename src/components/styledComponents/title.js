import styled from 'styled-components';

const Title = styled.text `
  font-family: Righteous;
  font-size: 150px;
  text-shadow: -3px 0 grey, 0 3px black, 3px 0 white, 0 -1px grey;
`;

export default Title;
